# Codex Tasks

Codex's future work comprises X01–X08: concrete enemy modules, placeholder Boss attacks, sacrifice UI and data, debug/telemetry, and automated test expansion.

Codex must wait until Claude freezes and documents each relevant core interface. Each task then uses one `codex/<task-id>-<task-name>` branch, stays inside its approved scope, tests against the released contract, and opens one PR to `develop`.
